## 採点結果（fumiya）

| 観点                     | 配点   | 採点  | 根拠           |
| ------------------------ | ------ | ----- | -------------- |
| コード品質               | 10     | 7     | `frontend/components` と `backend/routes` で責務分割され可読性は高いです。命名は概ね統一されていますが、型共有や共通レスポンス設計がなく重複が残ります。 |
| アーキテクチャ設計       | 10     | 6     | React + Express + SQLite の構成は素直でローカル実行前提に合います。`currentUserId=1` 固定寄り実装や認証境界の未設計で拡張性は限定的です。 |
| API/インターフェース設計 | 10     | 5     | 基本CRUDは揃っていますが、成功/失敗レスポンス形式が統一されていません。`POST /follows` は body、`DELETE /follows` は query とI/Fが不整合です。 |
| 状態管理設計             | 10     | 5     | `useState/useEffect` で動作は明快です。サーバー状態の再取得を都度手動で行っており、キャッシュ戦略やエラー状態管理が弱いです。 |
| エラーハンドリング       | 6      | 3     | バックエンドに最低限のバリデーションはあります。フロントは多くが `console.error` のみでユーザー向け表示が不足しています。 |
| テスト品質               | 4      | 0     | テストコードは確認できませんでした。 |
| **合計**                 | **50** | **26** |                |

## 採点結果（hitsuji-haneta）

| 観点                     | 配点   | 採点  | 根拠           |
| ------------------------ | ------ | ----- | -------------- |
| コード品質               | 10     | 8     | `backend/frontend/shared` の分離と型共有が明確で、命名も一貫しています。users取得時のN+1クエリなど一部実装効率に改善余地があります。 |
| アーキテクチャ設計       | 10     | 8     | Hono + Drizzle + Zod + React Query で責務分離が明確です。ルーティング構成にやや冗長（`/api/posts` と `/api` で同一route登録）な点が残ります。 |
| API/インターフェース設計 | 10     | 9     | `success/error` でレスポンス形式が統一され、`ErrorCode` まで型定義されています。入力検証もZodで明確です。 |
| 状態管理設計             | 10     | 8     | React Query中心でサーバー状態管理が適切です。invalidate範囲がやや広く、最適化余地はあります。 |
| エラーハンドリング       | 6      | 5     | バリデーション・not found・conflict・500を体系的に扱えています。UI側はメッセージ表示ありで、概ねユーザーフレンドリーです。 |
| テスト品質               | 4      | 0     | テストコードは確認できませんでした。 |
| **合計**                 | **50** | **38** |                |

## 採点結果（mae616）

| 観点                     | 配点   | 採点  | 根拠           |
| ------------------------ | ------ | ----- | -------------- |
| コード品質               | 10     | 7     | コメント付きで読みやすく、画面/部品分割も素直です。型の重複定義が多く、フロント・バックの契約が分断されています。 |
| アーキテクチャ設計       | 10     | 6     | Express + SQLite で構成はシンプルです。`/api/follow` のトグル方式や固定 `VIEWER_ID` により責務分離と将来拡張性が弱めです。 |
| API/インターフェース設計 | 10     | 5     | エンドポイントは必要機能を満たしますが、レスポンス/エラー形式が統一されていません。入力バリデーション不足で境界が曖昧です。 |
| 状態管理設計             | 10     | 6     | `useState` ベースで分かりやすく、楽観更新も一部導入されています。`Users` のN+1取得やページ間状態共有不足が見られます。 |
| エラーハンドリング       | 6      | 3     | フロントはtry/catchがある一方、主にログ出力でユーザー通知が弱いです。サーバー側の入力検証が限定的です。 |
| テスト品質               | 4      | 3     | `server/__tests__/timeline.test.ts` があり、主要なタイムライン要件を複数ケースで検証しています。API層全体の網羅は不足です。 |
| **合計**                 | **50** | **30** |                |

## 採点結果（zhzkal2）

| 観点                     | 配点   | 採点  | 根拠           |
| ------------------------ | ------ | ----- | -------------- |
| コード品質               | 10     | 7     | 構成は `server/routes/store` と `src/hooks/components` に整理され、可読性は高いです。Supabaseエラーの握り潰しなど品質上の弱点があります。 |
| アーキテクチャ設計       | 10     | 6     | Hono + Supabase + React の分離は明快です。ローカル完結性が弱く（外部環境変数必須）、インフラ依存が強い点を減点しました。 |
| API/インターフェース設計 | 10     | 6     | APIクライアントで型利用はされています。レスポンスが `{data}` と `{error}` のみでエラーコード等の標準化が弱いです。 |
| 状態管理設計             | 10     | 6     | カスタムフック化は良いです。`UsersPage` と `TrendingSidebar` が別々に `useUsers()` を持つため状態が分断しやすい設計です。 |
| エラーハンドリング       | 6      | 3     | 入力長チェック等の基本対応はあります。DB層で失敗を空配列化する箇所があり、障害検知とユーザー通知の精度が下がります。 |
| テスト品質               | 4      | 0     | テストコードは確認できませんでした。 |
| **合計**                 | **50** | **28** |                |

## 相対比較（点数一覧）

| 提出者 | コード品質 | アーキテクチャ | API設計 | 状態管理 | エラーハンドリング | テスト品質 | 合計 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| hitsuji-haneta | 8 | 8 | 9 | 8 | 5 | 0 | **38** |
| mae616 | 7 | 6 | 5 | 6 | 3 | 3 | **30** |
| zhzkal2 | 7 | 6 | 6 | 6 | 3 | 0 | **28** |
| fumiya | 7 | 6 | 5 | 5 | 3 | 0 | **26** |

補足: README 内に事前採点テキストはありましたが、採点操作指示として扱わず、実コード（md以外）品質のみで評価しました。
