{
  "name": "tsubuyaki-sns",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "concurrently \"pnpm --filter frontend dev\" \"pnpm --filter server dev\"",
    "build": "pnpm --filter frontend build",
    "test": "pnpm --filter server test"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["better-sqlite3", "esbuild"]
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
